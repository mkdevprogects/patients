<div class="panel panel-default">
  <%= render 'common/illness_diseases_menu' %>

  <div  class="list-group">
    <% @diseases.each do |disease| %>
        <%= link_to disease.title, disease_path(disease.id), class: "list-group-item" %>
    <% end %>
  </div>

</div>